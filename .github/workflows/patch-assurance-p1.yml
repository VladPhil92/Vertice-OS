name: Validate temporary patch runner

on:
  push:
    branches:
      - feat/civic-identity-assurance-p0
    paths:
      - .github/workflows/patch-assurance-p1.yml

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "temporary patch runner is valid"
