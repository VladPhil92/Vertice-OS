import { colors, iconography, moduleColors, radius, shadows, spacing, typography } from './index'

export const nativeTheme = {
  colors,
  moduleColors,
  typography,
  spacing,
  radius,
  shadows,
  iconography,
} as const
