{
  "name": "@vertice/ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --port 8001",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8001 --workers 4",
    "test": "pytest",
    "lint": "ruff check .",
    "format": "ruff format .",
    "typecheck": "mypy ."
  }
}
