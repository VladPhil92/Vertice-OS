apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: vertice
  labels:
    app: vertice-os
    component: api
spec:
  type: ClusterIP
  selector:
    app: vertice-os
    component: api
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
