apiVersion: v1
kind: Service
metadata:
  name: ai
  namespace: vertice
  labels:
    app: vertice-os
    component: ai
spec:
  type: ClusterIP
  selector:
    app: vertice-os
    component: ai
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
