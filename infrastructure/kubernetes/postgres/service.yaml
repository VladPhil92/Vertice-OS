apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: vertice
  labels:
    app: vertice-os
    component: postgres
spec:
  type: ClusterIP
  selector:
    app: vertice-os
    component: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
