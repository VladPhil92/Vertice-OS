apiVersion: v1
kind: Service
metadata:
  name: neo4j
  namespace: vertice
  labels:
    app: vertice-os
    component: neo4j
spec:
  type: ClusterIP
  selector:
    app: vertice-os
    component: neo4j
  ports:
    - name: http
      port: 7474
      targetPort: 7474
      protocol: TCP
    - name: bolt
      port: 7687
      targetPort: 7687
      protocol: TCP
