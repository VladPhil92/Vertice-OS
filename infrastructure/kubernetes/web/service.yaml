apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: vertice
  labels:
    app: vertice-os
    component: web
spec:
  type: ClusterIP
  selector:
    app: vertice-os
    component: web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
