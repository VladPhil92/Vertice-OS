apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: vertice
  labels:
    app: vertice-os
    component: redis
spec:
  type: ClusterIP
  selector:
    app: vertice-os
    component: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
